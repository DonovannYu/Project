This project stores student records in a binary file (students.dat) using fwrite() and fread(). Binary storage was chosen because it allows an entire Student structure to be saved and loaded with a single function call, resulting in simpler code and efficient storage. The project is organized using a header file for abstraction, a separate implementation file for the registry functions, and a main.c file for the menu and user interaction.
